/* eslint-disable */
import React from 'react';
import { storiesOf } from '@storybook/react';
import CreditCard from './CreditCard';

storiesOf('CreditCard', module).add('default', () => <CreditCard />);
